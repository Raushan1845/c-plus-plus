#include <iostream>
using namespace std ;
// code for array by calling function

void printarray(int a[],int n ) {
	for(int i =0 ; i<n ; i++) {
		cout<<a[i]<<endl;
	}
}
int main()
{
	int b[] = {2,4,7,8,3,1,32};
	int m = sizeof(b) / sizeof(int);

	printarray(b, m);

	return 0;
}
